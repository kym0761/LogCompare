# GSS Project 개발 및 운영 에이전트 지침 (AGENTS.md)

## 1. 프레임워크 및 런타임
- **PRD/README 우선**: 서브 프로젝트의 `PRD.md`/`README.md`에 명시된 타깃 런타임(.NET, Python 등) 최우선 준수. (불명확 시 임의 최신 버전 적용 금지)
- **오프라인 의존성 보장**: 폐쇄망 배포 환경을 고려하여 빌드 출력물에 종속성 라이브러리(DLL 등) 누락 방지 (예: `<CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>`).

## 2. 프로젝트 구조 및 명명
- **폴더 네이밍**: 서브 프로젝트는 `01_ProjectName`, `02_...` 형태의 2자리 순번 접두사 사용.
- **독립성 보장**: 각 서브 프로젝트는 자체 빌드/설정 파일(`csproj`, `requirements.txt` 등)을 갖추고 독립 빌드 가능하도록 구성.

## 3. 문서화 규칙
- **`PRD.md` 최우선 준수**: 기능 명세, 입출력 규격, 예외 처리 규칙을 개발 기준으로 최우선 준수.
- **PRD 변경 동기화**: 요구사항이 기존 `PRD.md`에 없거나 변경 필요 시, 사용자 확인 후 `PRD.md`를 선제 갱신하고 작업 진행.
- **README 갱신 제어**: 개발 작업 중 `README.md`는 자동 갱신하지 않으며, 사용자의 명시적 요청 시 또는 신규 서브 프로젝트 최초 생성 시에만 작성/갱신.

## 4. 보안 (Zero-Credential)
- **자격 증명 하드코딩 금지**: 실서버 IP, 계정, 비밀번호, API Key 등은 소스 코드에 기재 금지. 외부 설정(`targets.json`, `appsettings.json` 등)으로 분리.
- **더미 템플릿 자동 생성**: 설정 파일 부재 시 실행 시점에 주석 포함 더미 템플릿(`YOUR_USERNAME` 등) 자동 생성.
- **`.gitignore` 관리**: 실서버 접속 정보, 작업 로그(`.log`), 빌드 바이너리(`.exe`, `.dll` 등) 커밋 방지.

## 5. 현장 운영 안전 가드 (Fail-Safe)
- **백업 & 롤백 필수**: 대상 파일/설비 수정 전 `.origin` 원본 백업 선행, 실패 시 자동 롤백 지원.
- **버전 가드 (Skip)**: 이미 처리되었거나 조건 불일치 설비는 덮어쓰지 않고 안전하게 스킵.
- **작업 보고서 자동 저장**: 콘솔 출력 외 실행 결과 요약 로그(`patch_report_YYYYMMDD.log` 등) 자동 파일 저장.
